<h2>Edit newsletter</h2>

<%= render "form.html", changeset: @changeset,
                        action: newsletter_path(@conn, :update, @newsletter) %>

<%= link "Back", to: newsletter_path(@conn, :index) %>
