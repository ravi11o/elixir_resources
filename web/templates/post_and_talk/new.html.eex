<h2>New post and talk</h2>

<%= render "form.html", changeset: @changeset,
                        action: post_and_talk_path(@conn, :create) %>

<%= link "Back", to: post_and_talk_path(@conn, :index) %>
